# Managed via ~/Developer/environment/.ssh/config (stow symlink).
# Put machine-specific or sensitive host entries in ~/.ssh/config.local.
Include ~/.ssh/config.local

Host github.com
  AddKeysToAgent yes
  UseKeychain yes
  IdentityFile ~/.ssh/id_ed25519
