set -g prefix C-Space
set -g prefix2 C-b
bind C-Space send-prefix

bind q source-file ~/.config/tmux/tmux.conf \; display-message "tmux config reloaded"

setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

unbind '"'
bind - split-window -v -c "#{pane_current_path}"

unbind %
bind | split-window -h -c "#{pane_current_path}"

bind h split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"

unbind c
bind c new-window -n "environment" -c "#{pane_current_path}" \; send-keys -t 1 "nvim" C-m \; split-window -t 1 -h -p 50 -c "#{pane_current_path}" \; send-keys -t 2 "opencode" C-m \; split-window -t 1 -v -f -l 15 -c "#{pane_current_path}" \; select-pane -t 1

bind -n C-M-Left select-pane -L
bind -n C-M-Right select-pane -R
bind -n C-M-Up select-pane -U
bind -n C-M-Down select-pane -D

bind -n C-M-S-Left resize-pane -L 5
bind -n C-M-S-Right resize-pane -R 5
bind -n C-M-S-Up resize-pane -U 5
bind -n C-M-S-Down resize-pane -D 5

set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"
set -g mouse on
set -g history-limit 100000
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g escape-time 0
set -g focus-events on
set -g set-clipboard on
set -g detach-on-destroy off
set -g window-size latest
setw -g aggressive-resize on
set -g window-style "bg=default"
set -g window-active-style "bg=default"

set -g status-position top
set -g status-style "bg=default,fg=default"
set -g status-left "#[fg=black,bg=blue,bold] #S #[bg=default] "
set -g status-right "#[fg=blue]#{?client_prefix,PREFIX ,}#[fg=brightblack]#h "
set -g window-status-format "#[fg=brightblack] #I:#W "
set -g window-status-current-format "#[fg=blue,bold] #I:#W "
set -g pane-border-style "fg=colour8,bg=default"
set -g pane-active-border-style "fg=colour8,bg=default"
